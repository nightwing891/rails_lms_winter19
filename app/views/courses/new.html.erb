<h1>Add Course</h1>

<%= form_with(model: @course, local: true, class: "ui form") do |f| %> 
<%# form_for @course do |f| %>
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, required: true, placeholder: 'Name' %>
  </div>

  <%= f.submit nil, class: "ui inverted blue button" %>
<% end %>